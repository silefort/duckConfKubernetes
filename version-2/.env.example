# Choisir le node-binder à utiliser : "python" ou "go"
# Par défaut, aucun node-binder n'est lancé si ce fichier n'existe pas
# Copiez ce fichier en .env et décommentez la ligne souhaitée

# COMPOSE_PROFILES=python
# COMPOSE_PROFILES=go
